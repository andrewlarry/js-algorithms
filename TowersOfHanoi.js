/**
 * You have 3 towers and N disks of different sizes which can slide onto any tower.
 * The puzzle starts with disks sorted in ascending order of size from top to bottom.
 * 
 * Constraints:
 * * Only one disk can be moved at a time.
 * * A disk is slid off the top of one tower to another tower.
 * * A disk cannot be placed on top of a smaller disk.
 * 
 * Write a function to move the disks from the first tower to the last using stacks.
 * 
 */